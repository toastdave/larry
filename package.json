{
	"name": "larry",
	"private": true,
	"packageManager": "bun@1.3.10",
	"workspaces": ["apps/*", "packages/*"],
	"scripts": {
		"dev": "bun run --cwd apps/web dev",
		"build": "bun run --cwd apps/web build",
		"check": "bun run --cwd apps/web check && bun run --cwd packages/db check && bun run --cwd packages/ai check && bun run --cwd packages/search check",
		"lint": "biome check .",
		"format": "biome format --write .",
		"test": "bun run --cwd apps/web test && bun run --cwd packages/db test && bun run --cwd packages/ai test && bun run --cwd packages/search test",
		"db:generate": "bun run --cwd packages/db generate",
		"db:migrate": "bun run --cwd packages/db migrate",
		"ops:alerts": "bun run --cwd packages/db ops:alerts",
		"db:push": "bun run --cwd packages/db push",
		"db:studio": "bun run --cwd packages/db studio",
		"seed": "bun run --cwd packages/db seed",
		"docker:up": "docker compose up -d postgres mailpit",
		"docker:down": "docker compose down",
		"dev:docker": "docker compose up -d --build"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4"
	}
}
